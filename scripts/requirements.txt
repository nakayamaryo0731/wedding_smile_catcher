# Requirements for utility scripts
# Install with: pip install -r scripts/requirements.txt

# LINE Bot SDK (same version as Cloud Functions)
line-bot-sdk==3.23.1

# Google Cloud Libraries
google-cloud-firestore==2.28.0
google-cloud-storage==3.12.0

# Image processing (for rich menu setup, image restore)
Pillow>=12.2.0

# Image restore (perceptual hash matching for restore_event_images.py)
imagehash==4.3.2
scipy>=1.18.0
numpy>=2.5.1
requests>=2.34.2

# Environment variables
python-dotenv>=1.2.2
