# Development and testing dependencies for Wedding Smile Catcher

# Testing framework
pytest>=9.0.3
pytest-cov>=4.1.0           # Code coverage
pytest-mock>=3.12.0         # Mocking utilities
pytest-asyncio>=1.3.0      # Async testing support

# Mocking and testing utilities
responses>=0.25.0           # Mock HTTP requests

# Code quality
ruff>=0.15.12                 # Linter and formatter (replaces black + flake8)
mypy>=1.20.2                 # Type checker

# Load testing (install separately: pip install locust)
# locust is excluded from CI due to heavy dependencies (numpy build issues)
# For local load testing, run: pip install locust>=2.32.0

# Include production dependencies
-r requirements.txt
