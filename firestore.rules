rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access
    match /{document=**} {
      allow read, write: if false;
    }

    // Users collection
    // Frontend can read user names for display
    // Admin can delete users (authenticated via anonymous auth)
    match /users/{userId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;
    }

    // Images collection
    // Frontend can read images and scores for ranking display
    // Admin can delete images (authenticated via anonymous auth)
    match /images/{imageId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;
    }

    // Events collection (for multi-event support)
    // Frontend can read event metadata
    // Admin can delete events (authenticated via anonymous auth)
    match /events/{eventId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;

      // Event-specific users subcollection
      match /users/{userId} {
        allow read: if true;
        allow delete: if request.auth != null;
        allow create, update: if false;
      }

      // Event-specific images subcollection
      match /images/{imageId} {
        allow read: if true;
        allow delete: if request.auth != null;
        allow create, update: if false;
      }
    }
  }
}
