rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access
    match /{document=**} {
      allow read, write: if false;
    }

    // Users collection
    // Frontend can read user names for display
    // Authenticated users (including anonymous) can delete for admin panel
    match /users/{userId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;
    }

    // Images collection
    // Frontend can read images and scores for ranking display
    // Authenticated users (including anonymous) can delete for admin panel
    match /images/{imageId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;
    }

    // Events collection (for multi-event support)
    // Frontend can read event metadata
    // Authenticated users (including anonymous) can delete for admin panel
    match /events/{eventId} {
      allow read: if true;
      allow delete: if request.auth != null;
      allow create, update: if false;

      // Event-specific users subcollection
      match /users/{userId} {
        allow read: if true;
        allow delete: if request.auth != null;
        allow create, update: if false;
      }

      // Event-specific images subcollection
      match /images/{imageId} {
        allow read: if true;
        allow delete: if request.auth != null;
        allow create, update: if false;
      }
    }
  }
}
