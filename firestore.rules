rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access
    match /{document=**} {
      allow read, write: if false;
    }

    // Users collection
    // Frontend can read user names for display
    // Deletion is disabled for security (use Firebase Console for manual deletion)
    match /users/{userId} {
      allow read: if true;
      allow delete, create, update: if false;
    }

    // Images collection
    // Frontend can read images and scores for ranking display
    // Deletion is disabled for security (use Firebase Console for manual deletion)
    match /images/{imageId} {
      allow read: if true;
      allow delete, create, update: if false;
    }

    // Events collection (for multi-event support)
    // Frontend can read event metadata
    // Deletion is disabled for security (use Firebase Console for manual deletion)
    match /events/{eventId} {
      allow read: if true;
      allow delete, create, update: if false;

      // Event-specific users subcollection
      match /users/{userId} {
        allow read: if true;
        allow delete, create, update: if false;
      }

      // Event-specific images subcollection
      match /images/{imageId} {
        allow read: if true;
        allow delete, create, update: if false;
      }
    }
  }
}
