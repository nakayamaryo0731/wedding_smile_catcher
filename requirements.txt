# Production dependencies for Wedding Smile Catcher

# Google Cloud Platform (use latest compatible versions)
google-cloud-vision>=3.15.0
google-cloud-firestore>=2.29.0
google-cloud-storage>=2.14.0
google-cloud-logging>=3.16.0
# Provides the `vertexai` module; the standalone `vertexai` package pins
# an old aiplatform whose protobuf<6 conflicts with google-cloud-firestore>=2.29.0
google-cloud-aiplatform>=1.165.1

# Image processing
Pillow>=12.3.0
imagehash>=4.3.1

# LINE Bot SDK
line-bot-sdk>=3.25.0

# Cloud Functions
functions-framework>=3.10.2

# Utilities
requests>=2.34.2
tqdm>=4.70.0
